import React from 'react'
import ReactDOM from 'react-dom/client'
import ReactFormSaverDemo from './react-demo'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ReactFormSaverDemo />
  </React.StrictMode>,
)
